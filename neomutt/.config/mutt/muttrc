## Mailbox Setup

set folder = "~/.local/share/mail/raffaele.nickp@gmail.com"

mailboxes =Inbox =Sent =Trash =Education =Drafts
set spoolfile = "+Inbox"
set record = "+Sent"
set trash = "+Trash"

## General Settings

set mailcap_path = /home/nick/.config/mutt/mailcap
auto_view text/plain
alternative_order text/enriched text/plain text/html

set markers = no		      # Disables the `+` displayed at line wraps
set mark_old = no		      # Unread mail stay unread until read
set mime_forward = yes		# attachments are forwarded with mail
set wait_key = no		      # mutt won't ask "press key to continue"

set signature = '~/.config/mutt/signature'
set sort = 'reverse-date'
set display_filter = "tac | sed '/\\\[-- Autoview/,+1d' | tac" # Supress Autoview Text

set date_format= "%Y-%m-%d %H:%M"

# set index_format= "%2C %Z %?X?A& ? [%D] - %F - %s (%c)"
set index_format=" %zs %zc %zt | %-46.46L  %?X?& ? %.60s %?M10?(%1M)&? %> %{!%a %d %h %H:%M}      "

set pager_index_lines=10
set sidebar_visible = yes
set sidebar_width = 20
set sidebar_short_path = yes
set sidebar_next_new_wrap = yes
set mail_check_stats
set sidebar_format = '%D%?F? [%F]?%* %?N?%N/? %?S?%S? '

## Color

# source ~/.config/mutt/dracula.muttrc
# source ~/.config/mutt/gruvbox/colors-gruvbox-shuber.muttrc
# source ~/.config/mutt/gruvbox/colors-gruvbox-shuber-extended.muttrc
# source ~/.config/mutt/gruvbox/gruvbox-sheoak.muttrc

## Bindings and Macros

bind index,pager i noop
bind index,pager g noop

bind index,pager \Ck sidebar-prev       # Ctrl-Shift-P - Previous Mailbox
bind index,pager \Cj sidebar-next       # Ctrl-Shift-N - Next Mailbox
bind index,pager \Co sidebar-open       # Ctrl-Shift-O - Open Highlighted Mailbox

macro index,pager A  "<save-message>=Trash<enter>"
macro index O "<shell-escape>mbsync -a<enter>"

bind pager <up> previous-line           
bind pager <down> next-line               
bind pager K previous-line              
bind pager J next-line                  # Naviagtion 
bind index j next-entry
bind index k previous-entry
bind index D delete-message
bind index \031 previous-undeleted	    # Mouse wheel
bind index \005 next-undeleted		      # Mouse wheel
bind pager \031 previous-line		        # Mouse wheel
bind pager \005 next-line		            # Mouse wheel
bind index G last-entry
bind index gg first-entry

bind index,pager S sync-mailbox
bind attach l view-mailcap

